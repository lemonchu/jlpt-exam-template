# 验证记录

构建读取当前 YAML、实测约束和字体子集，不读取原始试卷 PDF。当前源码内置原版子集及现有题目所需的少量同款补充字形，不含完整字体。

## 当前内置子集版本

在只含公开源文件和已声明子集的干净目录中验证：没有原始 PDF、`resources/user-fonts/` 或完整字体缓存，`fonts.yaml` 的五个路径均为 `null`。

| 题册 | 页数 | 完整字体使用 | 同款补充字形使用 |
| --- | ---: | ---: | ---: |
| A 文字 | 33 | 0 | 0 |
| A 听力 | 15 | 0 | 0 |
| B 文字 | 33 | 0 | 583 |
| B 听力 | 15 | 0 | 30 |

四册均实际编译通过，96 页的字形顺序、Unicode 和坐标核验通过，最大坐标误差小于 0.000176 bp。与调整前成品对照的 6 个代表页在 144 dpi 下均为 0 像素变化，并已查看对照图。这是 6 页图片抽查，不能称为 96 页逐像素比较。

- 82 个原版子集的文件散列未改变，共 1,778,864 字节。
- 两个同款补充子集共 150,428 字节：Ryumin 覆盖实际绘制的 252 字及排版测量所需的两个括号；FutoGo 覆盖实际绘制的 24 字及测量所需的数字 1–7。另保留半角和全角空格。
- 补充子集的轮廓和横向度量与对应完整字体一致；来源及精确字符范围见 `profiles/n1-original/sample-fonts.json`。
- 测试新增“龘”时，构建明确提示配置对应的 Ryumin 完整字体，没有自动替换字体。

机器可读结果见 [bundled-font-builds.json](validation/source-release/bundled-font-builds.json)。

## 调整前的四册基线

| 题册 | PDF 页数 | 绘制字形 | 正式印刷四选一题 | 对应完整版补字 | 通用替代字体 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A 文字 | 33 | 16945 | 70 | 0 | 0 |
| A 听力 | 15 | 3018 | 15 | 0 | 0 |
| B 文字 | 33 | 15322 | 69 | 583 | 0 |
| B 听力 | 15 | 2781 | 15 | 30 | 0 |

听力数量是印在题册上的选择题数量，不是音频总题数。B 的原稿只有 69 道正式文字题，本项目未补造第 70 题。

四册的全部场景命令和语义文字绑定与既有精确组件版本一致。`tools/check_builds.py` 验证了输入快照、每个字形的顺序、Unicode 和坐标，最大坐标误差小于 0.001 bp。B 的新增字形仍使用对应的 Ryumin / FutoGo 完整字体。

实际以 Poppler 144 dpi 比较了 6 个代表页面：A 文字的封面、首个内容页和末页，A 听力首个内容页，B 文字第 18 页，B 听力末页；每页变化像素均为 0。此前 A 全部 48 页的逐像素回归也为 0，但本次没有把这 6 页抽查写成新的 48 页图片检查。

## 本地字体准备

- 从此前完整项目导入全部 82 个原版子集，SHA-256 校验通过。
- 从匹配的 N1V.pdf 在独立目录恢复 12 个字体，轮廓、Unicode 映射和横向度量与已验证子集一致。
- `--check` 通过；人工修改字宽的字体被拒绝。
- 原字体缺失时显示准备命令，不静默换字体。
- 模拟 FutoGo 缺字而 ShinGo 有该字时，构建路由报告缺字，不跨原字体代替。

`--pdf-dir` 已实现四科恢复入口，但本次仅有 N1V.pdf 的独立源文件可用于重建验证；G/R/L 的 PDF 恢复路径尚未逐科实测。从已有项目导入四科字体的路径已全部验证。

## 源文件检查与 CI

`python3 tools/check_project.py` 使用随附子集，无需完整字体，检查 A/B YAML、题组及素材引用、原字体配置、子集哈希和字符范围，并确认发布文件中没有完整字体或手工打包文件。

GitHub Actions 在 dev/test 提交或 PR 时检查源码，并使用内置子集编译 A/B 四册；不下载完整字体、不安装替代字体、不生成 ZIP 发布物。工作流配置本身不代表线上 CI 已通过；实际结果以 GitHub Actions 中对应提交的运行记录为准。

## 使用范围

上述结果针对当前 A/B 内容及本地字体版本。现代完整版与原 PDF 的旧 CID 字形存在轮廓差别；任意新字、新文章长度或不同历史版本的字体，不承诺逐像素相同。完整字体及本地恢复方法见 [docs/FONTS.md](docs/FONTS.md)。
